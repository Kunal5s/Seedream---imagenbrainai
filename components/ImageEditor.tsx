import React from 'react';

// This component has been disabled as per user request to remove the Image Editing feature.
const ImageEditor: React.FC = () => {
  return null;
};

export default ImageEditor;
